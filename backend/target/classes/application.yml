spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Parcauto-OneTECH
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

    mail:
        host: smtp.gmail.com
        port: 587
        username: rabiezouita82@gmail.com
        password: aenjkckfzdkkgrud
        properties:
          mail:
            smtp:
              auth: true
              starttls:
                enable: true
              ssl:
                enable: false
